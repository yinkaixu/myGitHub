function del_stock(code)
{
	 if(confirm('确定要删除"'+code+'"吗?'))
 	{
 		
 		//document.formak.code.vaule=code;
 		//alert(document.formak.code.vaule);
 		//document.formak.submit();
 		myform = document.getElementById('formak');
 		myform.elements["code"].value = code
 		//alert(myform.elements["code"].value);
 		myform.submit();
 		return true;
 	}
 	return false;
}


function del_result(ret)
{
	if(ret=='1')
	{
			alert('del ok')
		    document.location.reload
	}
	else
	{
	    alert(result)
	}
}